[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "echo '--- RUNNING SCHEMA PUSH ---' && NODE_TLS_REJECT_UNAUTHORIZED=0 npx drizzle-kit push --force 2>&1 && echo '--- SCHEMA PUSH DONE ---' || echo '--- SCHEMA PUSH FAILED ---'; npm start"
